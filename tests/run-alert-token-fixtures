#!/usr/bin/env bash
set -euo pipefail
ROOT_DIR="$(cd "$(dirname "$0")/.." && pwd)"
cd "$ROOT_DIR"

STATE_FILE="$ROOT_DIR/.run/pinky-beacon.state"
ALERT_FILE="$ROOT_DIR/.run/pinky-beacon.alert"

mkdir -p "$ROOT_DIR/.run"
rm -f "$ALERT_FILE"

./bin/pinky-set-state stopped >/tmp/pinky-alert-token-stopped.out
if [[ -f "$ALERT_FILE" ]]; then
  echo "FAIL alert token file should not exist after stopped reset"
  exit 1
fi

./bin/pinky-set-state brain fixture-token-1 >/tmp/pinky-alert-token-brain-1.out
state="$(cat "$STATE_FILE")"
alert="$(cat "$ALERT_FILE")"
if [[ "$state" != "brain" || "$alert" != "fixture-token-1" ]]; then
  echo "FAIL expected brain fixture-token-1 got state=$state alert=$alert"
  exit 1
fi

./bin/pinky-set-state brain fixture-token-1 >/tmp/pinky-alert-token-repeat.out
repeat_alert="$(cat "$ALERT_FILE")"
if [[ "$repeat_alert" != "fixture-token-1" ]]; then
  echo "FAIL repeated token changed unexpectedly: $repeat_alert"
  exit 1
fi

./bin/pinky-set-state brain fixture-token-2 >/tmp/pinky-alert-token-brain-2.out
new_alert="$(cat "$ALERT_FILE")"
if [[ "$new_alert" != "fixture-token-2" ]]; then
  echo "FAIL new token was not written: $new_alert"
  exit 1
fi

./bin/pinky-set-state stopped >/tmp/pinky-alert-token-final-stop.out
echo "PASS alert-token-fixtures"
